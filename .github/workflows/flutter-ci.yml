name: Flutter CI

on:
  push:
    branches: [main]
    paths:
      - 'lib/**'
      - 'test/**'
      - '.github/workflows/flutter-ci.yml'
  pull_request:
    branches: [main]
    paths:
      - 'lib/**'
      - 'test/**'
      - '.github/workflows/flutter-ci.yml'

jobs:
  analyze-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: dart-lang/setup-dart@v1
        with:
          sdk-version: 3.11.5

      - name: Install Flutter
        uses: flutter-actions/setup-flutter@v4
        with:
          version: 3.41.9

      - run: flutter pub get

      - run: flutter analyze

      - run: flutter test

  build:
    needs: analyze-and-test
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            artifact_name: flutter_app_windows_x64
            build_cmd: flutter build windows --release
            artifact_path: build/windows/x64/runner/Release/flutter_app.exe
          - os: macos-latest
            artifact_name: flutter_app_macos
            build_cmd: flutter build macos --release
            artifact_path: build/macos/Build/Products/Release/flutter_app.app
          - os: ubuntu-latest
            artifact_name: flutter_app_linux
            build_cmd: flutter build linux --release
            artifact_path: build/linux/x64/release/bundle/

    steps:
      - uses: actions/checkout@v4

      - uses: dart-lang/setup-dart@v1
        with:
          sdk-version: 3.11.5

      - name: Install Flutter
        uses: flutter-actions/setup-flutter@v4
        with:
          version: 3.41.9

      - run: flutter pub get

      - name: Install Linux build dependencies
        if: matrix.os == 'ubuntu-latest'
        run: |
          sudo apt-get update
          sudo apt-get install -y libgtk-3-dev libsecret-1-dev keybinder-3.0 libayatana-appindicator3-dev

      - name: Build ${{ matrix.artifact_name }}
        run: ${{ matrix.build_cmd }}

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_name }}
          path: ${{ matrix.artifact_path }}
          retention-days: 5